# Salesforce Agentforce integration. Copy to .env.local and fill in.
# Never commit real values for the two secret vars below.

SF_AGENT_ID=
SF_MY_DOMAIN_URL=

SF_CONSUMER_KEY=
SF_CONSUMER_SECRET=

# Optional override; defaults to https://api.salesforce.com if unset.
# SF_AGENT_API_BASE=
